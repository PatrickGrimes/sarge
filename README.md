# SARN'T (static)

Unzip into the **root** of `PatrickGrimes/sarge` so `index.html` sits next to `workouts/` and `images/`. Commit and push.

Pages: Settings → Pages → Deploy from branch `main` / folder `/ (root)`.

Bookmarks:

- https://patrickgrimes.github.io/sarge/pat/
- https://patrickgrimes.github.io/sarge/pam/

Morning bots overwrite `workouts/pat.json` and `workouts/pam.json`, then push.
